services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: orderqr
      POSTGRES_PASSWORD: orderqr
      POSTGRES_DB: orderqr
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
